buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.2'
    }
}

allprojects {
    // This block is no longer needed since repositories are declared in settings.gradle
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
